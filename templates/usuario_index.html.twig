<!DOCTYPE html>
<html lang="en">
  {% include 'head.html.twig' %}
  <body>
  {% include 'navbar.html.twig' %}
  <div class="container">
    <h2>Administracion de Usuarios</h2>
    <p>En esta pantalla podemos hacer las operaciones de ABM para Usuaruos del sistema. </p>
    <table class="table">
      <thead>
        <tr>
          <th>Email</th>
          <th>Nombre de usuario</th>
          <th>Nombre</th>
          <th>Apellido</th>
          <th>Bloqueado</th>
          <th>Clave</th>
        
        </tr>
      </thead>
      <tbody>
          {% for usuario in usuarios %}
        <tr>
          <td>{{usuario.email}}</td>
          <td>{{usuario.username}}</td>
          <td>{{usuario.first_name}}</td>
          <td>{{usuario.last_name}}</td>
           <td>{{(usuario.activo == 0 ? 'Sí' : 'No')}}</td>
           <td>{{usuario.password}}</td>
          <div class="container">
            <td><a class="btn btn-warning" href="./index.php?action=usuario_editar&usuario_email={{usuario.email}}" onclick="return confirm('¿Estás seguro que querés editar este usuario?');">editar</a></td>
            <td><a class="btn btn-danger" href="./index.php?action=usuario_eliminar&usuario_email={{usuario.email}}" onclick="return confirm('¿Estás seguro que querés borrar este usuario?');">Borrar</a></td>
              <td><a class="btn btn-default" href="./index.php?action=usuario_bloquear&usuario_email={{usuario.email}}">
              {% if usuario.activo == 0 %}
              Desbloquear
              {% else %}
              Bloquear
              {% endif %}
              </a></td>

          </div>
        </tr>
{% endfor %}
      </tbody>

    </table>

   Paginas:
      {% for i in 1..10 %}
        <a class="btn btn-primary" href="algoparahacer index">{{i}}</a>
      {% endfor %}
    

  </div>



  {% include 'footer.html.twig' %}
  </body>
</html>
