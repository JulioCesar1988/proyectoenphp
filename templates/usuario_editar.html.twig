<!DOCTYPE html>
<html lang="en">
  {% include 'head.html.twig' %}
  <body>
  {% include 'navbar.html.twig' %}
  {% include 'header.html.twig' %}
    <div class="row">
      <div class="col-md-offset-4 col-md-4" style="text-align: center;">
        <h4>Editar usuario: {{old_email}} </h4>
        <form action="./index.php?action=usuario_update&old_email={{old_email}}" method="post">
          
            
            <div class="form-group">
            <label>Email</label>
            <input class="form-control" type="email" name="email" value={{u.email}} required><br>
          </div>
          <div class="form-group">
            <label>Usuario</label>
            <input class="form-control" type="text" name="username" value={{u.username}} required><br>
          </div>
          <div class="form-group">
            <label>Password</label>
            <input class="form-control" type="password" name="pwd" value={{u.password}} required><br>
          </div>
          <div class="form-group">
            <label>Nombre</label>
            <input class="form-control" type="text" name="first_name" value={{u.first_name}} required><br>
          </div>
          <div class="form-group">
            <label>Apellido</label>
            <input class="form-control" type="text" name="last_name" value={{u.last_name}} required><br>
          </div>
          <div class="form-group">
            <label>Roles</label>
            <br>
            Roles setteados: ({% for rol in this_user_roles %}
              {{rol}}
            {% endfor %})<br>
            {% for rol in roles %}
              {{rol.name}}
              <input type="checkbox" name="rol[]" value="{{rol.nombre}}"><br>
            {% endfor %}
          </div>
  
          <input type="submit" value="Actualizar">
        </form>
      </div>
    </div>
  {% include 'footer.html.twig' %}
  </body>
</html>